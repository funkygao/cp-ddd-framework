#!/bin/bash
#====================================================
# 变更热点，展示哪些类经常被修改
# 一个类经常改动，可能意味着违反单一职责
#
# Usage：在某个代码库的根目录执行本脚本
#====================================================

topN=25
git log --name-only --pretty="format:" | grep -v -e '^$' | sort | uniq -c | sort -r | head -n $topN

